% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maybe.R
\name{maybe}
\alias{maybe}
\alias{mosiya}
\title{Search similar names from existing data}
\usage{
maybe(x, len = 1, min_dist = 4, min_dist_norm = 0.2)

mosiya(x, len = 6, min_dist = 3, min_dist_norm = 0.2)
}
\arguments{
\item{x}{A String or a vector of strings to be checked.}

\item{len}{integer 1: when checking scientific name, 6: when checking wamei (Japanese name).}

\item{min_dist}{Minimum editing distance. Less than min_dist will be output.}

\item{min_dist_norm}{Minimum normalised editing distance.}
}
\value{
Tibble.
}
\description{
maybe() for scientific names, mosiya() for wamei (Japanese names).
Both compare the input against the wamei checklist, and keep the
candidates that are close enough.
}
\details{
maybe() is the body and mosiya() is a thin wrapper of it.
The two differ only in the defaults: \code{len} selects the reference
(6 for wamei, otherwise scientific names) and \code{min_dist} is looser
for scientific names, which are longer.

The candidates are picked in C++ (\code{editdist_close_pairs()}), so the
whole \code{length(x) * length(reference)} table is never built in R.
}
\section{Functions}{
\itemize{
\item \code{maybe()}: Search similar scientific names from existing data

\item \code{mosiya()}: Search similar wamei (Japanese names) from existing data

}}
\examples{
library(wameicheckr)

x <- c("Viola madahuricaa", "Carex nevarta")
maybe(x)

x <-
  c("\u30cf\u30c3\u30ab\u30b0\u30b5",
    "\u30b9\u30ba\u30ce\u30a8\u30f3\u30c9\u30a6") |>
  stringi::stri_unescape_unicode()
mosiya(x)

}
\seealso{
editdist_multi()
}
